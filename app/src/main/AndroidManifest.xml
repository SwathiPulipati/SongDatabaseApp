<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.finalprojecttake2">

    <uses-permission android:name = "android.permission.RECORD_AUDIO"/>
    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FinalProjectTake2">
        <activity
            android:name=".EditSongInfo"
            android:exported="true"
            android:theme="@style/Theme.FinalProjectTake2.Popup"/>
        <activity
            android:name=".SongDetails"
            android:exported="true"
            android:theme="@style/Theme.FinalProjectTake2.Popup" />
        <activity
            android:name=".AddSongActivity"
            android:exported="true"
            android:theme="@style/Theme.FinalProjectTake2.Popup" />
        <activity
            android:name=".BottomNavigationActivity"
            android:exported="true"
            android:label="@string/title_activity_bottom_navigation"
            android:usesCleartextTraffic="true"/>
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.FinalProjectTake2.Popup">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>